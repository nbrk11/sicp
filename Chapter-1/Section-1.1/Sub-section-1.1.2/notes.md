# Notes

## Quick notes

In LISP there is a special keyword *define* is used to bind the value to a name

```Scheme
(define pi 3.14)
```

In order to keep track of those bindings there is a thing called environment (global, though there are plenty of them).
